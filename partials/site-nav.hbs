{{#if en}}
    <a href="{{@site.url}}/en">
        <span class="lang-switch" >de</span></a>
    <div id="language-tooltip">{{t "Change The Language"}}</div>
{{else}}
    <a href="{{@site.url}}">
        <span class="lang-switch" >en</span></a>
    <div id="language-tooltip">Switch To English</div>
{{/if}}

<ul class="menu">
    {{#if @site.navigation}}
        {{#if en}}
            {{> "navigation-en"}}
        {{else}}
            {{navigation}}
        {{/if}}
    {{/if}}
    {{#is "post"}}
        <span class="nav-post-title {{#unless @site.logo}}dash{{/unless}}">{{post.title}}</span>
    {{/is}}
</ul>
